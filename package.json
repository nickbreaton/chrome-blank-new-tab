{
  "name": "chrome-blank-new-tab",
  "private": true,
  "license": "MIT",
  "scripts": {
    "prestart": "rimraf dist/development",
    "start": "webpack --mode development --watch --devtool sourcemap",
    "prebuild": "rimraf dist/production*",
    "build": "webpack --mode production --output-path dist/production",
    "postbuild": "cd dist && bestzip production.zip production"
  },
  "devDependencies": {
    "bestzip": "^1.1.4",
    "copy-webpack-plugin": "^4.4.1",
    "rimraf": "^2.6.2",
    "webpack": "^4.0.0-beta.1",
    "webpack-cli": "^2.0.6"
  },
  "dependencies": {
    "material-components-web": "^0.30.0",
    "redux": "^3.7.2"
  }
}
